# Build a vertical slice first

BFitLog will begin implementation with a minimal vertical slice rather than isolated UI, backend, or sync work. The first slice should prove login, offline body weight logging, sync to the self-hosted backend, and a simple chart across Expo Android and Expo Web, because this exercises the riskiest product and architecture requirements together.
