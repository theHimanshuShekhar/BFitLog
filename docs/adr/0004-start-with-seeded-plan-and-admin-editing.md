# Start with a seeded plan and reserve editing for admins

BFitLog will start with the existing 4-day plan seeded into the database, while treating plan editing as an admin capability rather than a normal workout-logging flow. This keeps v1 focused on viewing, logging, and stats, but leaves room for an admin-only editor once seeded-data changes become inconvenient.
