# Support offline logging with limited scope

BFitLog v1 will support creating workout logs, set logs, and body weight logs offline, then syncing them to the self-hosted server later. Offline scope is limited to logging and cached plan text; admin plan editing and guaranteed offline exercise media are explicitly out of scope for v1 because they would require heavier conflict handling and media caching.
