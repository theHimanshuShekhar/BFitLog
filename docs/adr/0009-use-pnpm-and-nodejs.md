# Use pnpm and Node.js

BFitLog will use pnpm workspaces and Node.js for the TypeScript monorepo. This fits Expo, Hono, Drizzle, Docker-based deployment, and shared package workflows while avoiding an additional runtime choice during the offline-sync spike.
